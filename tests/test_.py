import pytest
from unittest.mock import patch, MagicMock

# Test case 1: test hàm format_timestamp 
def test_kieu_du_lieu(tmp_path):
    print("kieu du lieu cu no la", type(tmp_path))
    assert False
